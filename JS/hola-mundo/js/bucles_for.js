'use strict'

//Bucle FOR
//Estructura de Control que se repirte varias veces

var length = 100;

for (let index = 0; index <= length; index++) {
    console.log("Vamos por el #: " + index);

    // debugger; 
}